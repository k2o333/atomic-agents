# PostgreSQL configuration for Synapse Platform
# Optimized for performance with JSONB operations and extensions

# Connection settings
listen_addresses = '*'
port = 5432
max_connections = 100

# Memory settings
shared_buffers = 256MB
effective_cache_size = 1GB
work_mem = 4MB
maintenance_work_mem = 64MB

# Checkpoint settings
checkpoint_completion_target = 0.9
checkpoint_segments = 32
checkpoint_timeout = 15min

# WAL settings
wal_buffers = 16MB
default_statistics_target = 100

# Extension settings
shared_preload_libraries = 'age'

# Query planner settings (optimized for JSONB)
seq_page_cost = 1.0
random_page_cost = 1.1
cpu_tuple_cost = 0.01
cpu_index_tuple_cost = 0.005
cpu_operator_cost = 0.0025

# Logging settings
log_statement = 'none'
log_min_duration_statement = 1000
log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h '
log_checkpoints = on
log_connections = on
log_disconnections = on
log_lock_waits = on

# Autovacuum settings
autovacuum = on
autovacuum_max_workers = 3
autovacuum_naptime = 1min
autovacuum_vacuum_threshold = 50
autovacuum_analyze_threshold = 50

# Extension-specific settings for pgvector
# (These would be set after pgvector is installed)
# shared_preload_libraries = 'age, vector'

# Extension-specific settings for AGE
# (These are configured in the Dockerfile with shared_preload_libraries = 'age')